/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
    images: {
        domains: ["avatars.githubusercontent.com", "assets.aceternity.com", "ui.aceternity.com"],
      },
};

export default nextConfig;
